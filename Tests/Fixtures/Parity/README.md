See `docs/PARITY.md` for the one-time setup procedure that populates this
directory with `neutral.tif` and the per-preset exports (`contrast_p50.tif`,
`exposure_p1.tif`, …) needed to un-gate `ParityTests`.

Everything in this directory except this README and `manifest.json` is
personal-photo TIFF output and stays untracked.
